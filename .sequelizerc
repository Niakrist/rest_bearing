import path from "path";
import { fileURLToPath } from "url";

const __dirname = path.dirname(fileURLToPath(import.meta.url));

// Убедитесь, что ваш config.js экспортируется в формате CommonJS (require)
// Временно закомментируйте  импорт и используйте require.
// import config from "./config/config.js";
const config = require("./config/config.js").default;

export default {
  config: path.resolve(__dirname, "config", "config.js"),
  "migrations-path": path.resolve(__dirname, "migrations"),
  "seeders-path": path.resolve(__dirname, "seeders"),
  "models-path": path.resolve(__dirname, "models"),
};
